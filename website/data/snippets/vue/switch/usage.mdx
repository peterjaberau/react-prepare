```html
<script setup>
  import * as ibrainsdSwitch from "@ibrains-design/switch"
  import { normalizeProps, useMachine } from "@ibrains-design/vue"
  import { computed } from "vue"

  const [state, send] = useMachine(ibrainsdSwitch.machine({ id: "1" }))

  const api = computed(() =>
    ibrainsdSwitch.connect(state.value, send, normalizeProps),
  )
</script>

<template>
  <label v-bind="api.getRootProps()">
    <input v-bind="api.getHiddenInputProps()" />
    <span v-bind="api.getControlProps()">
      <span v-bind="api.getThumbProps()" />
    </span>
    <span v-bind="api.getLabelProps()">
      <span v-if="api.checked">On</span>
      <span v-else>Off</span>
    </span>
  </label>
</template>
```
