```jsx
import * as {{component}} from "@ibrains-design/{{component}}"
import { useMachine, normalizeProps } from "@ibrains-design/react"

function {{titleCase component}}() {
  const [state, send] = useMachine({{component}}.machine({ id: "1" }))

  const api = {{component}}.connect(state, send, normalizeProps)

  return (
    <div {...api.getRootProps()}>
      <div {...api.getControlProps()}>...</div>
    </div>
  )
}
```
