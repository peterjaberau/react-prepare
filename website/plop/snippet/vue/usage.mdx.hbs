```html
<script setup>
import * as {{component}} from "@ibrains-design/{{component}}"
import { useMachine, normalizeProps } from "@ibrains-design/vue"
import { computed } from "vue"

const [state, send] = useMachine({{component}}.machine({ id: "1" }))

const api = computed(() => {{component}}.connect(state.value, send, normalizeProps))
</script>

<template>
  <div ref="ref" v-bind="api.getRootProps()">
    <div v-bind="api.getControlProps()">...</div>
  </div>
</template>
```
